<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="10dp"
    app:cardCornerRadius="16dp"
    app:cardElevation="0dp"
    app:strokeWidth="1dp"
    app:strokeColor="?attr/colorOutlineVariant">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Row 1: DragHandle + Avatar + Issuer/Account + Copy + Delete -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginBottom="14dp">

            <!-- Drag handle — touch target to start reorder -->
            <ImageView
                android:id="@+id/dragHandle"
                android:layout_width="28dp"
                android:layout_height="44dp"
                android:src="@drawable/ic_drag_handle"
                android:scaleType="center"
                android:contentDescription="Drag to reorder"
                android:layout_marginEnd="8dp"
                app:tint="?attr/colorOnSurfaceVariant" />

            <!-- Colored avatar circle with issuer initial -->
            <TextView
                android:id="@+id/textViewAvatar"
                android:layout_width="44dp"
                android:layout_height="44dp"
                android:gravity="center"
                android:textSize="18sp"
                android:textStyle="bold"
                android:textColor="#FFFFFF"
                android:background="@drawable/bg_avatar_circle"
                android:layout_marginEnd="14dp"
                tools:text="G"
                tools:backgroundTint="@color/avatar_blue" />

            <!-- Issuer + account name stack -->
            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:orientation="vertical">

                <TextView
                    android:id="@+id/textViewIssuer"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textSize="15sp"
                    android:textStyle="bold"
                    android:textColor="?attr/colorOnSurface"
                    android:maxLines="1"
                    android:ellipsize="end"
                    tools:text="Google" />

                <TextView
                    android:id="@+id/textViewAccountName"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textSize="12sp"
                    android:textColor="?attr/colorOnSurfaceVariant"
                    android:maxLines="1"
                    android:ellipsize="end"
                    android:layout_marginTop="2dp"
                    tools:text="user@gmail.com" />

            </LinearLayout>

            <!-- Copy button -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/buttonCopy"
                style="@style/Widget.Material3.Button.IconButton"
                android:layout_width="44dp"
                android:layout_height="44dp"
                android:contentDescription="@string/copy_code"
                app:iconSize="22dp"
                app:iconTint="?attr/colorOnSurfaceVariant" />

            <!-- Delete button — circular red container background -->
            <ImageView
                android:id="@+id/buttonDelete"
                android:layout_width="44dp"
                android:layout_height="44dp"
                android:src="@drawable/ic_delete"
                android:background="@drawable/bg_delete_circle"
                android:padding="11dp"
                android:scaleType="fitCenter"
                android:layout_marginStart="6dp"
                android:contentDescription="@string/delete_account"
                app:tint="?attr/colorError" />

        </LinearLayout>

        <!-- Row 2: OTP code (large, monospace, primary color) -->
        <TextView
            android:id="@+id/textViewOtp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="38sp"
            android:fontFamily="monospace"
            android:textColor="?attr/colorPrimary"
            android:letterSpacing="0.08"
            android:layout_marginBottom="14dp"
            tools:text="123 456" />

        <!-- Row 3: Progress bar + timer circle -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <com.google.android.material.progressindicator.LinearProgressIndicator
                android:id="@+id/progressBarTimer"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginEnd="12dp"
                app:trackCornerRadius="4dp"
                app:trackThickness="6dp"
                app:trackColor="?attr/colorSurfaceVariant"
                app:indicatorColor="?attr/colorPrimary" />

            <!-- Timer — circular badge, size matches delete button -->
            <TextView
                android:id="@+id/textViewTimer"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:background="@drawable/bg_timer_chip"
                android:gravity="center"
                android:textSize="13sp"
                android:textStyle="bold"
                android:textColor="?attr/colorOnPrimaryContainer"
                android:minWidth="48dp"
                tools:text="28s" />

        </LinearLayout>

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>